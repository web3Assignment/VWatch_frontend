# VWatch Progress Tracker

## Completed
- [x] Initialized Vite + React + TS + Tailwind project.
- [x] Implemented Stitch UI design system tokens (Geist, JetBrains Mono, Dark Mode variables).
- [x] Created type definitions for User, Room, Participant, Player, and Chat.
- [x] Built Mock Service Layer (`auth`, `room`, `participant`, `socket`).
- [x] Set up React Context for State Management (`AuthContext`, `RoomContext`).
- [x] Built shared UI components (`GlassCard`, `Button`, `Input`, `Modal`, `Navbar`, etc.).
- [x] Built Pages (`Landing`, `Login`, `Register`, `Dashboard`, `Room`).
- [x] Integrated `react-youtube` for player sync.
- [x] Documented Architecture and API Contract.

## Pending (Next Phase)
- [ ] Backend implementation (Node.js/Go/Python).
- [ ] Swap out Mock Services for actual REST/Socket.io calls.
- [ ] OAuth integration (Google/GitHub).
- [ ] Implement Light Theme variant styles across components (currently defaults to Dark).
- [ ] Add emoji reaction animations to `ChatPanel`.
